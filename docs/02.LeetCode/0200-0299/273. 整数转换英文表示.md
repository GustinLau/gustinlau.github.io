---
id: EB36FA513FF2FCBF
title: 273、整数转换英文表示
date: 2022-01-09 21:58:01
permalink: /leetcode/integer-to-english-words
link: https://leetcode-cn.com/problems/integer-to-english-words
toc: false
categories:
  - LeetCode
tags:
  - 递归
  - 数学
  - 字符串
---

<Level type="hard"/>

## 题目

将非负整数 `num` 转换为其对应的英文表示。

## 示例

示例 1：

```text
输入：num = 123
输出："One Hundred Twenty Three"
```

示例 2：

```text
输入：num = 12345
输出："Twelve Thousand Three Hundred Forty Five"
```

示例 3：

```text
输入：num = 1234567
输出："One Million Two Hundred Thirty Four Thousand Five Hundred Sixty Seven"
```

示例 4：

```text
输入：num = 1234567891
输出："One Billion Two Hundred Thirty Four Million Five Hundred Sixty Seven Thousand Eight Hundred Ninety One"
```

## 提示

- 0 <= num <= 2<sup>31</sup> - 1

## 分析

按 `3` 位分割解析，添加单位。

## 解答

```javascript
/**
 * @param {number} num
 * @return {string}
 */
var numberToWords = function (num) {
  if (num === 0) {
    return 'Zero'
  }
  const singles = ['', 'One', 'Two', 'Three', 'Four', 'Five', 'Six', 'Seven', 'Eight', 'Nine']
  const teens = [
    'Ten',
    'Eleven',
    'Twelve',
    'Thirteen',
    'Fourteen',
    'Fifteen',
    'Sixteen',
    'Seventeen',
    'Eighteen',
    'Nineteen'
  ]
  const tens = ['', 'Twenty', 'Thirty', 'Forty', 'Fifty', 'Sixty', 'Seventy', 'Eighty', 'Ninety']
  const thousands = ['Hundred', 'Thousand', 'Million', 'Billion']
  const re = /(\d+)(\d{3})/
  let s = num.toString()
  while (re.test(s)) {
    s = s.replace(re, '$1' + ',' + '$2')
  }
  const ans = []
  const arr = s.split(',').reverse()
  for (let i = 0; i < arr.length; i++) {
    let n = +arr[i]
    let str = []
    if (Math.floor(n / 100)) {
      str.push(singles[Math.floor(n / 100)] + ' ' + thousands[0])
      n = n % 100
    }
    if (n >= 10) {
      // 20+
      if (Math.floor(n / 10) > 1) {
        str.push(tens[Math.floor(n / 10) - 1])
        n = n % 10
      } else {
        str.push(teens[n - 10])
        n = 0
      }
    }
    if (n > 0) {
      str.push(singles[n])
    }
    ans.unshift(str.join(' '))
  }
  for (let i = ans.length - 2; i > -1; i--) {
    if (ans[i] !== '') {
      ans[i] = ans[i] + ' ' + thousands[ans.length - i - 1]
    }
  }
  return ans.filter((s) => s !== '').join(' ')
}
```
