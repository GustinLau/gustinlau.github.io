---
id: 6A7A4FADE5FD9320
title: 14、最长公共前缀
date: 2021-12-02 19:10:48
permalink: /leetcode/longest-common-prefix
link: https://leetcode-cn.com/problems/longest-common-prefix
toc: false
categories:
  - LeetCode
tags:
  - 字符串
---

<Level type='easy'/>

## 题目

编写一个函数来查找字符串数组中的最长公共前缀。

如果不存在公共前缀，返回空字符串 `""`。

## 示例

示例 1：

```text
输入：strs = ["flower","flow","flight"]
输出："fl"
```

示例 2：

```text
输入：strs = ["dog","racecar","car"]
输出：""
解释：输入不存在公共前缀。
```

## 提示

- 1 <= strs.length <= 200
- 0 <= strs[i].length <= 200
- strs[i] 仅由小写英文字母组成

## 分析

遍历数组，记录当前的最大公共前缀，遇到空字符串或最大公共前缀不存在时直接返回。

## 解答

```javascript
/**
 * @param {string[]} strs
 * @return {string}
 */
var longestCommonPrefix = function (strs) {
  const n = strs.length
  let sameStr = strs[0]
  for (let i = 1; i < n; i++) {
    const str = strs[i]
    if (str.length === 0) {
      return ''
    }
    for (let j = 0; j < sameStr.length; j++) {
      if (str[j] !== sameStr[j]) {
        if (j === 0) {
          return ''
        }
        sameStr = str.substr(0, j)
        break
      }
    }
  }
  return sameStr
}
```
